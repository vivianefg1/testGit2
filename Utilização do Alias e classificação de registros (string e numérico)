Consulta em ordem de classificação e alias:

Select <campo1> as <apelido>, <campo2> as <apelido> 
From <nome_tabela>
Order by <nome_campo> [ASC | DESC]

Select Nome as 'Participante' 
from aluno;

Select Nome as 'Participante' 
from aluno;

Select Nome as 'Paciente', idade_Pac as 'idade, status as 'Situação'
from paciente;

Select Nome as 'Participante' 
from aluno
order by nome;

Select Nome, idade_Pac, status
from paciente
order by status;

Select Nome, idade_Pac, status
from paciente
order by 3;

Select Nome, idade_Pac, status
from paciente
order by status,nome;

Select Nome, idade_Pac, status
from paciente
order by 3,1;

Select Nome as 'Paciente', idade_Pac as 'Idade', status as 'Situação'
from paciente
order by Paciente, Idade
